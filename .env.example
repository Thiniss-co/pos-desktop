# Main-process API origin. Leave unset for the honest local `not_configured` foundation state.
# HTTP is permitted only for loopback development hosts; production origins must use HTTPS.
MAIN_VITE_POS_API_ORIGIN=http://127.0.0.1:8000

# Never place desktop tokens, device secrets, or production credentials in this file.
